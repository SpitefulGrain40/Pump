# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.gh-pages-test-worktree
.deploy-master-worktree
# Stray build output that must never be committed to the repo root again
# (production is served from docs/, not these). Anchored to root so real
# source dirs named test/ elsewhere are unaffected.
/assets/
/test/
.superpowers/
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
